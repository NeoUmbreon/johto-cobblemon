c.battle.is_pvw == false ? return 0;

for_each(t.player, c.players, {
    t.player = t.player.player;
    c.was_wild_capture ? { t.music = 4; } : { t.music = 5; };

    q.run_command('scoreboard players set ' + t.player.username + ' BattleEnd ' + t.music);
});