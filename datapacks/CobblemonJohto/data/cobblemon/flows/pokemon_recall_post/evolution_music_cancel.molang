t.owner = c.pokemon.owner;
t.owner.has_tag('Evolving') ? {
    t.owner.remove_tag('Evolving');
    q.run_command('execute as ' + t.owner.username + ' run function johto:sound/overwriterecord {track:"",duration:0}');
};