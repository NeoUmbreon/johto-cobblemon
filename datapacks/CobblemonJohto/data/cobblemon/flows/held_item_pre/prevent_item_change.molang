c.pokemon.owner.has_tag('BattleTower') || c.pokemon.owner.has_tag('BattleFactory') ? {
    q.run_command('tellraw ' + c.pokemon.owner.username + ' {"text":"You can\'t change held items right now!","color":"gray","italic":true}');
    q.cancel;
};