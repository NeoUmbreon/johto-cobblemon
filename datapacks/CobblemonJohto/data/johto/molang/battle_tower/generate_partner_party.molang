t.name = q.to_lower(q.npc.name);
q.get_variable(q.player.data.temp, t.name) != 0 ? return 0;

v.party_size = 4;
v.trainer.id = 300;
v.trainer.roster = t.name;

q.set_variable(q.player.data.temp, t.name, q.run_script('johto:generate_party'));

return 1;